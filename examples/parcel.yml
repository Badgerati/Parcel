---
packages:
- name: 7zip.install
  #provider: choco
  provider: scoop
  version: 19.0
  #os: linux
  #when: ($parcel.os.type -eq 'linux')

- name: curl
  provider: choco
  version: 7.55.1

- name: pester
  provider: psgallery
  version: 4.8.1